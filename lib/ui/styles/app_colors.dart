import 'package:flutter/material.dart';

class AppColors {
  // Base Palette
  static const white0 = Color(0xFFF8F8F8);
  static const white1 = Color(0xFFFFFFFF);
  static const black0 = Color(0xFF1E2128);
  static const black1 = Color(0xFF2D3139);
  static const grey = Color(0xFF949B96);
  static const grey2 = Color(0xFFCACACA);

  // Status Colors
  static const green = Color(0xFF05A945);
  static const red = Color(0xFFFF4848);
  static const blue = Color(0xFF319BEC);
  static const highlight = Color(0xFF0066FF);

  // Design Specific Primaries
  static const emergencyRed = Color(0xFFFF0000);
  static const emergencyRedDark = Color(0xFF8B0000);
  static const primaryTeal = Color(0xFF1F96AD);
  static const patientBlue = Color(0xFF174C82);

  // Surface & Background - Standard
  static const bgLight = Color(0xFFF9FAFB);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const bgDark = Color(0xFF111827);
  static const surfaceDark = Color(0xFF1F2937);

  // Surface & Background - Emergency
  static const bgEmergencyDark = Color(0xFF0F0101);
  static const surfaceEmergencyDark = Color(0xFF1A0505);

  // Surface & Background - Patient
  static const bgPatientDark = Color(0xFF0F172A);
  static const surfacePatientDark = Color(0xFF1E293B);

  // Surface & Background - Registration
  static const bgRegistrationDark = Color(0xFF0D1B1E);
  static const surfaceRegistrationDark = Color(0xFF132A2E);
}
